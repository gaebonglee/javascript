//구구단 출력
function gugudan(n) {
  var m = parselnt(n);

  if (isNaN(m) || m < 1 || m > 9) {
    alert("잘못입력하셨습니다.");
    return;
  }

  for(var i = 1, i = 9; i++){
    document.write(m + "x" + i "=" m*i + "<br>")
  }
}
